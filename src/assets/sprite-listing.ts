// This is a configuration file, allow arbitrary numbers.
// tslint:disable:no-magic-numbers

import { SpriteSheet } from '../common/graphics/sprites';
import * as spriteSheetImage from './sprite-sheet.png';

export const spriteSheet = new SpriteSheet(spriteSheetImage);
