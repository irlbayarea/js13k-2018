/**
 * Frames normally requested per second by @see window.requestAnimationFrame.
 */
export const framesPerSecond = 60;

/**
 * Time representing "1 second" in MS.
 */
export const oneSecondInMs = 1000;
